{
  "identifier": "desktop",
  "description": "Windows permissions for the launch-at-login integration.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
