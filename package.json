{
  "name": "music-player",
  "version": "0.22.139",
  "type": "module",
  "private": true,
  "description": "Native local-first music player (Tauri + Rust).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "node --test tests/*.test.mjs",
    "test:browser": "node tests/artwork-transition-browser.cjs && node tests/handle-icon-browser.cjs && node tests/volume-control-browser.cjs && node tests/ui-redesign-browser.cjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "playwright": "^1.63.0"
  }
}
